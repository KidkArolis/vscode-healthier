{
	"name": "vscode-healthier",
	"displayName": "Healthier Linter",
	"description": "An opinionated code style agnostic linter – a friendly companion to Prettier.",
	"version": "3.0.0",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/KidkArolis/vscode-healthier.git"
	},
	"bugs": {
		"url": "https://github.com/KidkArolis/vscode-healthier/issues"
	},
	"author": {
		"name": "Karolis Narkevicius",
		"email": "hey@kn8.lt",
		"url": "https://github.com/KidkArolis"
	},
	"publisher": "KidkArolis",
	"icon": "icon.png",
	"categories": [
		"Linters"
	],
	"keywords": [
		"javascript",
		"javascriptreact",
		"typescript",
		"typescriptreact",
		"vue"
	],
	"engines": {
		"vscode": "^1.69.0",
		"npm": ">=7.0.0"
	},
	"activationEvents": [
		"*"
	],
	"main": "./client/out/extension",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "JavaScript Healthier Style",
			"properties": {
				"healthier.enable": {
					"scope": "resource",
					"type": "boolean",
					"default": true,
					"description": "Controls whether JavaScript Healthier Style is enabled for JavaScript files or not."
				},
				"healthier.enableGlobally": {
					"scope": "resource",
					"type": "boolean",
					"default": false,
					"description": "Controls whether JavaScript Healthier Style is enabled globally for JavaScript files or not."
				},
				"healthier.nodePath": {
					"scope": "resource",
					"type": [
						"string",
						"null"
					],
					"default": null,
					"description": "A path added to NODE_PATH when resolving the healthier module."
				},
				"healthier._legacyModuleResolve": {
					"scope": "resource",
					"type": "boolean",
					"default": false,
					"description": "Uses the legacy module resolving."
				},
				"healthier.options": {
					"scope": "resource",
					"type": "object",
					"default": {},
					"description": "The healthier options object to provide args normally passed to JavaScript Healthier Style when executed from a command line."
				},
				"healthier.engine": {
					"scope": "resource",
					"type": "string",
					"enum": [
						"healthier",
						"ts-healthier"
					],
					"default": "healthier",
					"description": "Controls whether VSCode should use an alternate Healthier engine, like ts-healthier."
				},
				"healthier.trace.server": {
					"scope": "window",
					"type": "string",
					"enum": [
						"off",
						"messages",
						"verbose"
					],
					"default": "off",
					"description": "Traces the communication between VSCode and the JavaScript Healthier Style linter service."
				},
				"healthier.run": {
					"scope": "resource",
					"type": "string",
					"enum": [
						"onSave",
						"onType"
					],
					"default": "onType",
					"description": "Run the linter on save (onSave) or on type (onType)"
				},
				"healthier.autoFixOnSave": {
					"scope": "resource",
					"type": "boolean",
					"default": false,
					"description": "Turns auto fix on save on or off."
				},
				"healthier.workingDirectories": {
					"scope": "resource",
					"type": "array",
					"items": {
						"anyOf": [
							{
								"type": "string"
							},
							{
								"type": "object",
								"properties": {
									"directory": {
										"type": "string",
										"description": "The working directory to use if a file's path start with this directory."
									},
									"changeProcessCWD": {
										"type": "boolean",
										"description": "Whether the process's cwd should be changed as well."
									}
								}
							}
						]
					}
				},
				"healthier.validate": {
					"scope": "resource",
					"type": "array",
					"items": {
						"anyOf": [
							{
								"type": "string"
							},
							{
								"type": "object",
								"properties": {
									"language": {
										"type": "string",
										"description": "The language id to be validated by JavaScript Healthier Style"
									},
									"autoFix": {
										"type": "boolean",
										"description": "Whether auto fixes are provided for the language"
									}
								}
							}
						]
					},
					"default": [
						"javascript",
						"javascriptreact",
						"typescript",
						"typescriptreact"
					],
					"description": "An array of language ids which should be validated by JavaScript Healthier Style"
				},
				"healthier.usePackageJson": {
					"type": "boolean",
					"default": true,
					"description": "Activate JavaScript Healthier Style based on project's package.json settings, use globally installed healthier module if set to \"false\""
				},
				"healthier.treatErrorsAsWarnings": {
					"type": "boolean",
					"default": false,
					"description": "Any linting error reported by Healthier will instead be displayed as a warning within VS Code."
				}
			}
		},
		"commands": [
			{
				"title": "Fix all auto-fixable Problems",
				"category": "JavaScript Healthier Style",
				"command": "healthier.executeAutofix"
			},
			{
				"title": "Enable JavaScript Healthier Style for this Workspace",
				"category": "JavaScript Healthier Style",
				"command": "healthier.enable"
			},
			{
				"title": "Disable JavaScript Healthier Style for this Workspace",
				"category": "JavaScript Healthier Style",
				"command": "healthier.disable"
			},
			{
				"title": "Show Output Channel",
				"category": "JavaScript Healthier Style",
				"command": "healthier.showOutputChannel"
			}
		],
		"jsonValidation": [
			{
				"fileMatch": "package.json",
				"url": "http://json.schemastore.org/package"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -b",
		"watch": "tsc -b -w",
		"test": "npm run compile && sh ./scripts/e2e.sh",
		"postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
		"package": "vsce package",
		"lint": "cd client && npm run lint && cd ../server && npm run lint && cd ..",
		"format": "cd client && npm run format && cd ../server && npm run format && cd .."
	},
	"devDependencies": {
		"@types/mocha": "^9.1.1",
		"@types/node": "^18.6.1",
		"@vscode/test-electron": "^2.1.5",
		"mocha": "^10.0.0",
		"snazzy": "^9.0.0",
		"ts-standard": "^11.0.0",
		"typescript": "^4.7.4",
		"vsce": "^2.10.0"
	},
	"dependencies": {
		"async": "^3.2.4"
	}
}
